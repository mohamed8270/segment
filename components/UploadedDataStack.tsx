import { uploadImageType } from '@/types'
import React from 'react'

interface Props {
    uploaded: uploadImageType;
}

const UploadedDataStack = ({uploaded}: Props) => {
  return (
    <div>
        <div>
            
        </div>
        <div></div>
    </div>
  )
}

export default UploadedDataStack